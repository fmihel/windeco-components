/* eslint-disable no-return-assign */
import React, {
    useEffect, useState, Suspense, useRef,
} from 'react';


function App({
}) {

    return (
        <div>
            app..
        </div>
    );
}

const mapStateToProps = (state) => ({

});

//export default redux.connect(mapStateToProps)(App);
export default App;
